//Entry controlled loop - for and while

//Exit controlled loop - do while
let pr = prompt("Enter a number(stop when 0 is entered");
do {
  console.log(pr);
  pr = prompt("Enter a number(stop when 0 is entered");
} while (pr != 0);
